# Copy to .env.local and fill in.

# Password for the /admin dashboard. Pick something long.
ADMIN_PASSWORD=

# Random string used to sign the admin session cookie.
# Generate one with: openssl rand -hex 32
ADMIN_SECRET=

# Optional — move the SQLite file (e.g. to a mounted disk in production).
# DATABASE_PATH=/data/jcbeauty.db
